from setuptools import setup, find_packages

setup(
    name='helper',             # Name of your module
    version='0.1',             # Version number
    packages=find_packages(),  # Automatically find all packages in the directory
)
